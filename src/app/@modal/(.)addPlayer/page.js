'use client'

import { Flex, Loader, Collection } from '@aws-amplify/ui-react';
import { get } from '@aws-amplify/api-rest';
import { PlayerCard } from '@/ui-components';

export default function AddPlayer(){
    return(
        <Flex>
Test
        </Flex>
    );
}